{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [ "${authors}" ],
  "entrypoints": {
    "main": [
      {
        "adapter": "kotlin",
        "value": "com.theendercore.top_hats_and_monocles.TopHatsAndMonocles::init"
      }
    ],
    "client": [
      {
        "adapter": "kotlin",
        "value": "com.theendercore.top_hats_and_monocles.client.TopHatsAndMonoclesClient::init"
      }
    ]
  },
  "mixins": [
    "top_hats_and_monocles.mixins.json",
    {
      "config": "top_hats_and_monocles.client.mixins.json",
      "environment": "client"
    }
  ],
  "accessWidener": "top_hats_and_monocles.classtweaker",
  "depends": {
    "java": ">=${java}",
    "minecraft": "${version_range}",
    "fabricloader": "*",
    "fabric-api": "*",
    "fabric-language-kotlin": ">=${fabric_kotlin}",
    "fzzy_config": ">=${fzzy_config}"
  },
  "contact": {
    "homepage": "https://github.com/${github_repo}",
    "issues": "https://github.com/${github_repo}/issues",
    "sources": "https://github.com/${github_repo}",
    "discord": "${discord_invite}"
  },
  "license": "${license}",
  "icon": "assets/top_hats_and_monocles/icon.png"
}